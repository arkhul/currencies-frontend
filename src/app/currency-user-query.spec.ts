import { CurrencyUserQuery } from './currency-user-query';

describe('CurrencyUserQuery', () => {
  it('should create an instance', () => {
    expect(new CurrencyUserQuery()).toBeTruthy();
  });
});
